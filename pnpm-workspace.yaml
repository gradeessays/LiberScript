packages:
  - "apps/*"
  - "packages/*"

# Dedupe ioredis to a single version so the instance type matches bullmq's.
overrides:
  ioredis: 5.11.0

allowBuilds:
  '@prisma/client': true
  '@prisma/engines': true
  esbuild: true
  msgpackr-extract: true
  prisma: true
  sharp: true
